import React from 'react';

const Navbar = () => {
    return (
        <nav className="navbar">
            <div>
                <a href="/">Home</a>
            </div>
        </nav>
    );
};

export default Navbar;